package main

import "strings"
import "strconv"
import "fmt"

func main(){
	values := []string{}
	values = append(values , "123")
	values = append(values , "234")

	output = strconv.Itoa(len(values))

	fmt.printf("%v" , output)
	
}
